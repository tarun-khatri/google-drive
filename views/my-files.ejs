<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Files</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 dark:bg-gray-900 text-gray-800 dark:text-white">
  <div class="container mx-auto p-6">
    <h1 class="text-2xl font-bold mb-4">My Uploaded Files</h1>

    <% if (userFiles.length === 0) { %>
      <div class="p-4 bg-yellow-100 text-yellow-800 rounded">
        <p>No files uploaded yet.</p>
      </div>
    <% } else { %>
      <div class="bg-white dark:bg-gray-800 shadow rounded-lg p-4">
        <ul class="divide-y divide-gray-200 dark:divide-gray-700">
          <% userFiles.forEach((file) => { %>
            <li class="flex items-center justify-between py-4">
              <div>
                <p class="text-lg font-semibold"><%= file.name %></p>
              </div>
              <div class="flex space-x-4">
                <!-- View File Button -->
                <a href="<%= file.publicURL %>" target="_blank" class="text-blue-500 hover:text-blue-700">
                  <button class="bg-blue-500 hover:bg-blue-700 text-white py-1 px-3 rounded">
                    View
                  </button>
                </a>

                <!-- Download File Button -->
                <a href="<%= file.publicURL %>" target="_blank" download class="text-green-500 hover:text-green-700">
                  <button class="bg-green-500 hover:bg-green-700 text-white py-1 px-3 rounded">
                    Download
                  </button>
                </a>
              </div>
            </li>
          <% }) %>
        </ul>
      </div>
    <% } %>
  </div>
</body>
</html>
